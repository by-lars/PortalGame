#include "ECS/ECS.h"
#include "Core/Base.h"

namespace PGame {
	/*mespace ECS {
		int s_ComponentCounter = 0;


		Scene::Scene() {

		}

		Entity Scene::AddEntity() {
			m_Entities.push_back({ m_Entities.size(), ComponentMask() });
			pgDebug("Created new entity id = " << m_Entities.back().id);
			return m_Entities.back().id;
		}*/
	//}
}